query GetDataDrawingPipe {
    GetDataDrawingPipe {
        GroupPipeId
        GroupPipeName
        Pipes {
            BaseMax
            BaseMin
            Length
            Lines {
                Color
                Lines
            }
            PipeId
            PipeName
            Size
            TypeAlarmChannel
        }
    }
}
